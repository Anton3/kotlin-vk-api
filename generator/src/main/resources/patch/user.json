{
  "objects": {
    "users_user_min": {
      "type": "object",
      "properties": {
        "id": {
          "type": "integer",
          "description": "User ID"
        },
        "first_name": {
          "type": "string",
          "description": "User first name"
        },
        "last_name": {
          "type": "string",
          "description": "User last name"
        },
        "deactivated": {
          "type": "string",
          "description": "Returns if a profile is deleted or blocked",
          "enum": [
            "deleted",
            "banned"
          ]
        },
        "is_closed": {
          "description": "Is the profile hidden by privacy settings",
          "$ref": "#/definitions/base_bool_int"
        },
        "can_access_closed": {
          "description": "Can we see the profile if is_closed = 1 (e.g. if that user is our friend)",
          "$ref": "#/definitions/base_bool_int"
        }
      },
      "required": [
        "id",
        "first_name",
        "last_name",
        "is_closed"
      ],
      "additionalProperties": false
    }
  }
}
