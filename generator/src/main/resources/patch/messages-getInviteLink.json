{
  "methods": [
    {
      "name": "messages.getInviteLink",
      "description": "Get an invitation link for a chat",
      "access_token_type": [
        "user",
        "group"
      ],
      "parameters": [
        {
          "name": "peer_id",
          "description": "Destination ID",
          "type": "integer",
          "required": true
        },
        {
          "name": "reset",
          "description": "Pass 1 to generate a new link and reset previous one",
          "type": "boolean",
          "required": false
        },
        {
          "name": "group_id",
          "description": "For actions on behalf of a group with user token",
          "type": "integer",
          "required": false
        }
      ],
      "responses": {
        "response": {
          "$ref": "responses.json#/definitions/get_invite_link_response"
        }
      }
    }
  ]
}
